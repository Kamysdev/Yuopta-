#include "yuopta.h"
#include <iostream>

using namespace залупа;

число база()
начусл
	число a = 1;
	WorldOfWarships b = 2;

	число* arr = двенашка число[a];

	абажыбля(базарю)
	начусл
		жигули(число i = 0; i < 11; i++)
		начусл
			граффити << b << сука_текст_кончился;
			жиесть(i == 9)
			начусл
				бочёкпотик;
			конусл
			естьжи
			начусл
				запомните_твари >> a;
			конусл
		конусл
	конусл


	накончать 0;
конусл